node_modules/
.DS_Store
.planning/
supabase/

# Maintainer-local overlays (not part of upstream contribution contract)
.local/
.agent/
.claude.json
plan/
modified_plan.md

# Python caches
__pycache__/
*.pyc

# Credentials & runtime state
.env
.env.local
.env.*.local
credentials.json
token.json
gmail-sync-log.json
sync-log.json

# Build artifacts
deno.lock

# Local docker volumes (defensive — named volumes live outside repo, but
# binding to ./pg_data is a common dev shortcut and we never want it
# committed)
pg_data/
.venv/
.pytest_cache/
*.egg-info/
htmlcov/
.coverage

# Local DB dumps â never commit
backups/
*.sql.gz
*.dump
