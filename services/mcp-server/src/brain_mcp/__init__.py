"""brain_mcp — Python FastMCP server for the self-hosted Open Brain fork.

Public entry point is `brain_mcp.server`. Tools live under
`brain_mcp.tools` and register themselves with the FastMCP instance
via a small `register(mcp)` function.
"""

__version__ = "0.1.0"
