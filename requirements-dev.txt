# Top-level dev dependencies. Install with:
#   uv pip install --system -r requirements-dev.txt
# Or:
#   pip install -r requirements-dev.txt
#
# Per-service Python projects (e.g. services/mcp-server) carry their
# own pyproject.toml. This file holds the cross-cutting test toolchain
# that is shared across all services.

# Core test toolchain
pytest>=8.0
pytest-asyncio>=0.24
pytest-cov>=5.0

# Integration test fixtures: ephemeral pgvector-enabled Postgres via
# testcontainers. The Postgres extra pulls psycopg by default for the
# wait-for-port logic.
testcontainers[postgres]>=4.7

# psycopg v3 binary build — used inside the testcontainers fixture and
# in any direct SQL assertions from tests.
psycopg[binary]>=3.2

# httpx for HTTP-level tests against the MCP server.
httpx>=0.27

# YAML for reading config/features.yaml in unit tests.
PyYAML>=6.0
